"""DEPRECATED package shim — see docs/CANONICAL_PATH.md."""
import warnings

warnings.warn(
    "src.event_based is legacy; use the canonical path in docs/CANONICAL_PATH.md",
    DeprecationWarning,
    stacklevel=2,
)
